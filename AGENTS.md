# Agent notes (Priceflux)

Cursor agents: treat this repo as an incremental, PR-driven delivery.

1. **Progress:** `docs/DELIVERY.md` (done / in progress / next / WIP)
2. **How to run & limits:** `docs/USAGE.md`
3. **Always-on rules:** `.cursor/rules/priceflux-workflow.mdc`
4. **Git:** user commits; agent does not commit/push unless asked; merge to `main` via PR only

Start every session by reading `docs/DELIVERY.md` and checking `git status`.
